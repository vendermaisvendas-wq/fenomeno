services:
  - type: web
    name: buscaplace
    runtime: python
    pythonVersion: "3.12.3"
    buildCommand: pip install -r requirements.txt && python -c "import db; db.init_db()"
    startCommand: uvicorn web:app --host 0.0.0.0 --port $PORT
    plan: free
